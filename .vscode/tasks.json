{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run",
            "command": "go",
            "args": [
                "run",
                "build.go",
                "${workspaceRoot}"
            ]
        }
    ]
}